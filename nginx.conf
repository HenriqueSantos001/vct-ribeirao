server {
    listen 80;

    location / {
        root /app;
        try_files $uri $uri/ /index.html;
    }

    location /vct-ribeirao {
        root /app;
        try_files $uri $uri/ /index.html;
    }
}
